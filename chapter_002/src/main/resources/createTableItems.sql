CREATE TABLE items (id CHARACTER VARYING (2000) PRIMARY KEY,
                    name CHARACTER VARYING (2000),
                    description CHARACTER VARYING (2000),
                    created BIGINT);
